#ifndef TASKS_HPP
#define TASKS_HPP

#include <string>
#include <vector>
#include <iostream>

namespace arteev
{
  void task1(const char* order);
  void task2(const char* fileName);
  void task3();
  void task4(const char* order, int size);
}

#endif
